<!DOCTYPE html>
<html>
  <head>
    <title>RubyBlogStarterKit</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-100">
    <div class="container mx-auto px-4">
      <% if notice %>
        <div data-controller="flash" class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4 transition-opacity duration-1000" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>
      <% if alert %>
        <div data-controller="flash" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4 transition-opacity duration-1000" role="alert">
          <span class="block sm:inline"><%= alert %></span>
        </div>
      <% end %>

      <%= yield %>

      <% if user_signed_in? %>

        <div class="mt-4 text-gray-600">
          Logged in as <strong class="font-semibold"><%= current_user.email %></strong>.
          <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "mt-2 bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
        </div>
      <% end %>
    </div>
  </body>

</html>
